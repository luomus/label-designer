import { SearchFieldsPipe } from './search-fields.pipe';

describe('SearchFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
